[build]
command = "npx prisma generate && npm run build"
functions = "netlify/functions"
publish = "dist"

[functions]
node_bundler = "esbuild"

[dev]
framework = "vite"
targetPort = 5173
port = 8888
publish = "dist"
